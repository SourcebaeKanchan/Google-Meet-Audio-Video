 function ReactMic(){

      // defaults -> false (available in React-Mic-Gold)
           // defaults -> 4000 milliseconds.  The interval at which captured audio is returned to onData callback (available in React-Mic-Gold).

}

export default ReactMic